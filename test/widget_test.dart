import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Full app tests require Hive initialization; covered by integration tests.
    expect(true, isTrue);
  });
}
